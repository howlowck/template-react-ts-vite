import { {{ camelCase name }} } from './style.module.css'
import classnames from 'classnames'
import React from 'react'

type Prop = {
  // put props typings here. i.e. "className: string"
}

const {{ properCase name }}: React.FC<Prop> = () => {
  return (
    <div className={classnames({{ camelCase name }})}>
      <p>
        Replace me ({{ properCase name }} component in
        src/components/{{ subPath subpath }}{{ properCase name }}/index.ts)
      </p>
    </div>
  )
}

export default {{ properCase name }}
